import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react-swc';

export default defineConfig({
  plugins: [react()],
  build: {
    outDir: 'dist', // ✅ standard Vite output for Vercel
  },
  server: {
    open: false, // prevents "spawn xdg-open ENOENT" warnings
  },
});
